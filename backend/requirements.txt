fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1
httpx==0.28.1
openai==1.59.6
openpyxl==3.1.5
pandas==2.2.3
pdfplumber==0.11.5
sse-starlette==2.2.1

# База знаний закупок (PostgreSQL 16 + pgvector)
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10
pgvector==0.3.6

# crawl4ai (рендер страниц для SEARCH_PROVIDER=serper) вынесен в
# requirements-crawl.txt — он тяжёлый и НЕ нужен для SEARCH_PROVIDER=gemini/mock.
# fetch_price работает без него (источник помечается недоступным).

# Тесты
pytest==8.3.4
pytest-asyncio==0.25.2
